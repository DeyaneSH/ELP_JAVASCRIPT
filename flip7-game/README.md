# Flip7 Game

## Lancement

1. Installez les dépendances : `npm install`
2. Lancez le jeu : `node index.js`

## Structure

Le code source est dans `src/` et les logs sont dans `logs/game_history.txt`.